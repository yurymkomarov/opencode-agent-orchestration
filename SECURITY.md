# Security Policy

This repository provides local automation scripts for AI coding tools.
Do not run scripts blindly. Review them first.

Never use this setup in directories containing:
- production secrets
- SSH keys
- kubeconfigs
- cloud credentials
- real `.env` files

Report security concerns via GitHub issues or private contact.
